# stacked-tui

> **Beautiful TUI for gh-stack — visualize stacked PRs as interactive graph.**

```bash
npx stacked-tui show
npx stacked-tui show --json
```

Reads `git log --graph` and highlights stacked branches.

## Test

`npm test` 1/1

## License

Apache-2.0
